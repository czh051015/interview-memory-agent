# AGENTS.md — offerloop（PointLoop · 逐点）

## 你的角色
你是本项目的**开发执行者**（当前 = Claude Code）。计划与验收由 WorkBuddy 负责，决策由用户负责。
**你只实现用户当场指定的那份计划书**（形如 `docs/38-开发计划书-*.md` / `docs/39-修改计划书-*.md` / `docs/实施计划-*.md`，NN 为编号）圈定的目标，**不擅自扩大范围**；遇到计划未覆盖的架构 / 接口决策，停下问用户。

## 开工前（每次新会话必做，别一上来重读整个项目）
1. **确认本轮计划书**——用户会说"按 `docs/38…` / `docs/39…` 开发"。若用户没说清楚 / 计划书有歧义，先向用户确认要执行哪一份，别自己猜。
2. 读那份计划书 —— 目标、范围、验收标准、状态行（待拍板 / 已拍板 / 待复核 → 落地）。
3. 读 `docs/CHANGES.md` —— **顶部「当前快照」**（项目现状，精简）+ **流水区前 3 条**（对方近况）。
4. `git log --oneline -10` 核对真实变更；`git status` 确认**在途未提交改动**的归属（offerloop 工作区常有多份计划书文档与前后端改动未提交，先确认哪些不是本轮范围）。
5. 需要项目知识（架构 / 命令 / 设计约定 / 坑）→ 读 `README.md`；确实不够再读具体代码。

## 干完活（写档再走）
**每结束一轮工作 / 关闭对话前**，在 `docs/CHANGES.md` 流水区顶部按模板追加交接条目，**一轮可写多条**（改了多个独立事项就各写一条），四要素缺一不可：
- **对应计划**：本次执行的计划书（如 `docs/38 §3.2`）/ 计划外（说明理由）
- **改动**：改了哪些文件、动了什么逻辑
- **验证**：跑了什么命令、结果如何（没跑验证要明说）。常用命令：`python -m pytest -q`（tests/ 397 用例）、`ruff check src tests`、`python scripts/run_evals.py`（套件 score/decompose/demo/medium）
- **遗留**：未完成 / 风险 / 待 WorkBuddy 或用户决策的事
然后 commit，把短 hash 回填条目。**「当前快照」的当前主线若有变化（计划完成 / 新计划开跑），顺手覆盖更新。**

## 边界与习惯
- 只做**用户指定计划书**范围内的事；想做额外改进 → 记进条目的「遗留」，不要顺手实现。
- 计划书状态行为「待拍板 / 待复核」时：**只做已拍板部分**；需要用户拍板的点列出来等指示，别自己代拍。
- 「没做完什么」和「做完了什么」同等重要，遗留要写实，别让下轮基于错误前提开工。
- 改动遵循 README.md 的设计约定：评分 = 「漏点识别传感器」（只答"这个点写了没有、证据在哪"，不判好坏）；**LLM 不产出任何作为事实展示的证据字段**（防"材料顶包"）；门禁 / 示证双模式按 trusted 路由；录错题 / 回流 / 人审闸门等既有链路红线不动。
