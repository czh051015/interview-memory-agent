# 检索标注集校对清单（20 条）

> 只看你熟悉的方向（Agent / RAG / 线程池 / 记忆 / 幻觉 这些面试要考的）。
> 每条查三个问题：① 该召回里有没有标错的？② 有没有该召回但我漏掉的？③ 不该召回里有没有其实相关的？
> 发现问题直接说「第 X 条：哪道题标错 / 该加哪道」，我来改。

## 1. 查询：Agent 框架 多智能体 工具调用

### 该召回（我标的）
- [ ] 如果让你设计一个Agent要考虑哪些模块？
- [ ] Agent的短期长期记忆是怎么实现的？
- [ ] 有没有考虑用大模型自己排除api超时和报错？

### 不该召回（我标的）
- [ ] 什么是两阶段提交？
- [ ] 多线程写一个死锁

## 2. 查询：RAG 检索增强 知识库 召回

### 该召回（我标的）
- [ ] 出现幻觉怎么处理？
- [ ] 介绍一下完整的 RAG 流程，文档解析、切块、向量化、召回和重排分别怎么做？
- [ ] 在 RAG 系统中，如何评估检索质量？你提到了文档级召回率接近 99%、页面级召回率约 77%，请解释这两个指标的具体定义和计算方式，以及为什么页面级召回率低于文档级？

### 不该召回（我标的）
- [ ] 多线程写一个死锁
- [ ] 随便写一个单例模式

## 3. 查询：线程池 并发 多线程

### 该召回（我标的）
- [ ] 讲一下java线程池？
- [ ] 如果你重新设计一个线程池会怎么设计？
- [ ] 多线程写一个死锁

### 不该召回（我标的）
- [ ] 什么是两阶段提交？
- [ ] 出现幻觉怎么处理？

## 4. 查询：数据库 事务 一致性 消息队列

### 该召回（我标的）
- [ ] 多级缓存和数据库的数据一致性问题怎么解决？
- [ ] RocketMQ 事务消息的原理是什么？如何保证业务状态更新与消息发送的最终一致性？

### 不该召回（我标的）
- [ ] 出现幻觉怎么处理？
- [ ] 简单讲下 Transformer 底层原理？

## 5. 查询：提示词 Prompt 工程

### 该召回（我标的）
- [ ] 提示词具体是怎么做？
- [ ] 还有其他提示词吗？
- [ ] 不动基础模型、不微调，模型不按格式输出、乱加戏幻觉，有什么优化方案？

### 不该召回（我标的）
- [ ] 多线程写一个死锁
- [ ] 随便写一个单例模式

## 6. 查询：向量数据库 相似度 检索

### 该召回（我标的）
- [ ] 向量数据库是干嘛用的？
- [ ] 何判断向量的相似度?
- [ ] 除了余弦相似度，还了解其他相似度算法吗?

### 不该召回（我标的）
- [ ] 多线程写一个死锁
- [ ] 随便写一个单例模式

## 7. 查询：上下文压缩 长对话 token 超限 总结

### 该召回（我标的）
- [ ] 长时间编程任务很容易超出模型上下文窗口，你采用了什么上下文压缩机制？
- [ ] 参考 Claude Code 等产品后，你如何在压缩上下文的同时维持多轮任务的记忆和执行效率？
- [ ] 在 AI Coding Agent Runtime 项目中，你提到实现了 Append-only Session Tree、Compaction、Checkpoint 与 Fork/Rewind。请详细说明 Compaction 的具体策略是什么？在压缩上下文时，如何平衡信息保留与成本降低？你提到 P50 净降成本降低 56.44%，这个指标是如何计算的？
- [ ] 什么叫“快到上限了”?对话是怎么逐步叠加的?

### 不该召回（我标的）
- [ ] 讲一下java线程池？
- [ ] 手撕：实现一个支持 TTL 的 LRU Cache，要求 `get` 和 `put` 的平均时间复杂度为 `O(

## 8. 查询：Agent 长短期记忆 实现

### 该召回（我标的）
- [ ] Agent的短期长期记忆是怎么实现的？
- [ ] 请介绍你的长期记忆 + 短期记忆是怎么实现的？为什么需要记忆？
- [ ] 短期记忆的具体实现方式是什么?

### 不该召回（我标的）
- [ ] 讲一下java线程池？
- [ ] 展开介绍一下长视频理解项目：项目背景、要解决的问题，以及遇到的困难

## 9. 查询：幻觉 怎么处理 降低

### 该召回（我标的）
- [ ] 出现幻觉怎么处理？
- [ ] 如何降低 Agent & 模型幻觉？
- [ ] 如何控制模型的幻觉问题?

### 不该召回（我标的）
- [ ] 多线程写一个死锁
- [ ] 多级缓存和数据库的数据一致性问题怎么解决？

## 10. 查询：API 超时 重试 熔断 降级 幂等

### 该召回（我标的）
- [ ] 如果遇到api超时和报错怎么解决？
- [ ] 有没有考虑用大模型自己排除api超时和报错？
- [ ] 如果设备控制接口超时或返回错误，如何设计重试、熔断、幂等和降级？

### 不该召回（我标的）
- [ ] 多线程写一个死锁
- [ ] 简单讲下 Transformer 底层原理？

## 11. 查询：长视频理解 视频分析 模型选型 token

### 该召回（我标的）
- [ ] 展开介绍一下长视频理解项目：项目背景、要解决的问题，以及遇到的困难
- [ ] 视频分析使用了什么模型？具体是哪一个模型？
- [ ] 为什么选择这个模型？模型选型依据是什么？
- [ ] 一段十几分钟或五十分钟的视频，大约会消耗多少 Token？是否会超出上下文或免费额度？
- [ ] 为什么视频转写后的 Token 数量这么少？系统实际处理的是视频、音频，还是 ASR 转写文本？
- [ ] 如果不考虑 Token 消耗和成本，你最倾向使用什么模型完成这项工作？为什么？

### 不该召回（我标的）
- [ ] 讲一下java线程池？
- [ ] 多线程写一个死锁

## 12. 查询：代码搜索 关键词 vs 语义 Embedding

### 该召回（我标的）
- [ ] 基于关键词的命令行代码搜索与基于 Embedding/RAG 的代码搜索，各有什么优缺点？

### 不该召回（我标的）
- [ ] 展开介绍一下长视频理解项目：项目背景、要解决的问题，以及遇到的困难
- [ ] 随便写一个单例模式

## 13. 查询：Java 类加载 JVM 并发集合

### 该召回（我标的）
- [ ] 怎么把class文件加载到jvm中？
- [ ] 你使用的 Java 版本是多少？Java17 有哪些新特性？你用过哪些？
- [ ] Java 并发包线程安全集合（ConcurrentHashMap）底层原理？JDK1.7 和 1.8 区别？

### 不该召回（我标的）
- [ ] 出现幻觉怎么处理？
- [ ] 展开介绍一下长视频理解项目：项目背景、要解决的问题，以及遇到的困难

## 14. 查询：RAG 文档切分 chunking 父子块 分块策略

### 该召回（我标的）
- [ ] 在 Agentic RAG 平台中，你设计了 Root + Leaf Hierarchy Chunking 与 Leaf-only 检索架构。请解释为什么选择这种分块策略？相比传统分块有什么优势？在实现中如何保证父子块之间的关联和上下文回填的准确性？
- [ ] 在 Agentic RAG 平台中，你设计了 Root + Leaf Hierarchy Chunking 与 Leaf-only 检索架构。请解释为什么选择这种架构？在实现中，如何确保 Leaf 块与 Parent 块的关联关系在检索后能正确回填上下文？你提到文档级召回率接近 99%，这个指标是如何评测的？
- [ ] 项目用到 RAG 增强，文档切分你是怎么做的？固定长度切片截断语句有问题吗？语义切片优缺点？
- [ ] 介绍一下完整的 RAG 流程，文档解析、切块、向量化、召回和重排分别怎么做？

### 不该召回（我标的）
- [ ] 多线程写一个死锁
- [ ] 讲一下java线程池？

## 15. 查询：混合检索 RRF 重排 rerank 多路召回

### 该召回（我标的）
- [ ] 在 RAG 系统中，你使用了 bge-m3 Dense、BM25 Sparse、Milvus 双索引、RRF 与 CrossEncoder Rerank。请解释 RRF 的原理，以及为什么选择 RRF 而不是其他融合方法？在实现中，如何确定 RRF 的 k 值？
- [ ] 在 RAG 系统中，如何评估检索质量？你提到了文档级召回率接近 99%、页面级召回率约 77%，请解释这两个指标的具体定义和计算方式，以及为什么页面级召回率低于文档级？
- [ ] 基于关键词的命令行代码搜索与基于 Embedding/RAG 的代码搜索，各有什么优缺点？

### 不该召回（我标的）
- [ ] 展开介绍一下长视频理解项目：项目背景、要解决的问题，以及遇到的困难
- [ ] 随便写一个单例模式

## 16. 查询：Human-in-the-loop 审批 状态机 Checkpoint 中断恢复

### 该召回（我标的）
- [ ] 在 Agent Runtime 中，你实现了 Human-in-the-loop 审批状态机，基于 Eino Interrupt/Resume。请详细说明中断和恢复的机制？如何保证在审批被拒绝后，修改参数并从 Checkpoint 恢复执行时，上下文和状态的一致性？
- [ ] 在 Agent 开发实习中，你实现了 Human-in-the-loop 审批状态机，基于 Eino Interrupt/Resume。请详细说明中断和恢复的机制？如何保证在审批被拒绝后，修改参数并从 Checkpoint 恢复执行时，上下文和状态的一致性？
- [ ] 在 Agent Runtime 中，你实现了 Human-in-the-loop 审批状态机，基于 Eino Interrupt/Resume。请详细说明中断和恢复的机制？如何保证在审批被拒绝后，修改参数并从 Checkpoint 恢复执行时，上下文和状态的一致性？
- [ ] 在 Agent 开发实习中，你实现了 Human-in-the-loop 审批状态机，基于 Eino Interrupt/Resume。请详细说明中断和恢复的机制？如何保证在审批被拒绝后，修改参数并从 Checkpoint 恢复执行时，上下文和状态的一致性？

### 不该召回（我标的）
- [ ] 讲一下java线程池？
- [ ] 出现幻觉怎么处理？

## 17. 查询：temperature top_p 模型参数 采样

### 该召回（我标的）
- [ ] 了解大模型 API 参数 temperature、top_p 吗？分别作用是什么？
- [ ] temperature 设置为 0，输入固定输出是否一定完全相同？为什么？

### 不该召回（我标的）
- [ ] 展开介绍一下长视频理解项目：项目背景、要解决的问题，以及遇到的困难
- [ ] 多线程写一个死锁

## 18. 查询：Transformer 大模型 底层原理 注意力

### 该召回（我标的）
- [ ] 简单讲下 Transformer 底层原理？

### 不该召回（我标的）
- [ ] 讲一下java线程池？
- [ ] 手撕：实现一个支持 TTL 的 LRU Cache，要求 `get` 和 `put` 的平均时间复杂度为 `O(

## 19. 查询：SSE WebSocket 流式回复 长连接

### 该召回（我标的）
- [ ] SSE 和 WebSocket 有什么区别？智能助手的流式回复更适合使用哪一种？

### 不该召回（我标的）
- [ ] 多线程写一个死锁
- [ ] 展开介绍一下长视频理解项目：项目背景、要解决的问题，以及遇到的困难

## 20. 查询：Agent 评测 如何证明效果更好 对比

### 该召回（我标的）
- [ ] 如何科学评测 Agent 系统效果？新版本对比旧版本如何证明更好？
- [ ] 对比 React、Plan&Execute、Reflection、CoT 等不同 Agent 架构区别？
- [ ] 在 RAG 系统中，如何评估检索质量？你提到了文档级召回率接近 99%、页面级召回率约 77%，请解释这两个指标的具体定义和计算方式，以及为什么页面级召回率低于文档级？

### 不该召回（我标的）
- [ ] 展开介绍一下长视频理解项目：项目背景、要解决的问题，以及遇到的困难
- [ ] 随便写一个单例模式
