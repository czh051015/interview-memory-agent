# 诊断报告

> 累积式问题清单。每次诊断追加/更新，绝不重复同一条错误。
> 状态：待修复 / 已修复 / 已忽略

## 2026-08-13 诊断（`--view` Top 榜 + `search.py RAG` + `prioritize` 三连实测）

| 阶段 | 错误 | 描述 | 归因 | 状态 |
| --- | --- | --- | --- | --- |
| 01-面经预处理 | prioritize 重算崩溃 | `run_market.py prioritize` 跑到第 11 次嵌入请求时断连，整条命令报错退出；前 10 次请求（每次约 3 秒）都成功，是跑到中途断的 | 重算优先级要把全库约 370 条全部重新向量化（把文字变成可检索的数字指纹）再写回，一次跑要连发十几次请求；设计上没有任何重试，中途断一次就直接崩，已算好的结果也全部落空。断连的确切原因（本地嵌入服务挂了还是超时）日志被截断看不到，**待确认** | 待修复 |
| 01-面经预处理 | 搜索出现 3 条一模一样的「RAG」 | 搜 RAG 返回 3 条文本完全相同的 `RAG (RAG) [jd]`，sim=1.00 | JD 的关键词按「题目」逐条入库、一条存一条；「RAG」同时出现在华为/去哪儿/阿里 3 份 JD 里，就存成 3 条相同记录。条目编号按「公司+关键词」生成，跨公司不查重，去重机制对它们完全失效（ISSUES V4） | 待修复 |
| 01-面经预处理 | JD 关键词与面经题混池 | 搜索时 JD 的裸技能词（如「RAG」两个字）永远排在真实面经题前面，挤掉真正想找的内容 | 检索库只有一个，JD 技能词和几百字的面经题混存混搜；语义搜索天然偏爱短文本——越短的越容易和查询完全对上，所以裸关键词 sim=1.00 霸榜。设计上没把「技能标签」和「面试题」分成两个池子（ISSUES V5） | 待修复 |
| 01-面经预处理 | Top 榜混入行为题 | 高频考点榜第 4「自我介绍」4 题、第 5「项目介绍」4 题 | 所有面经题入库时一律标成「知识点」类，没有区分「技术考点 / 行为题 / 流程题」，榜单也不做分类过滤，行为题被当成考点统计（ISSUES V6） | 待修复 |
| 01-面经预处理 | Top 榜考点过碎 | 「Agent架构」「Agentic RAG」「Agent框架选型」本是同一考点，却分成 3 条各自上榜；342 个考点只并成 271 簇 | 打标签时让大模型自由发挥，没约束到一份固定考点清单；榜单归堆只认「拼写互相包含」，字面不同但语义相近的考点归不到一起（ISSUES V7） | 待修复 |
