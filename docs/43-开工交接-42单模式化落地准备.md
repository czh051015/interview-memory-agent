# 43 · 开工交接：docs/42「评分单模式化 + 采分点来源分层」落地准备

日期：2026-09-07 · 交接方：WorkBuddy → 执行 Agent（Claude Code 等）· 状态：**待用户说「做」后执行**
任务依据：**docs/42（已拍板，P-A~P-D = ②/B1/①/①）**——本文档只做「开工准备清单」，实现细则一律以 docs/42 为准；两者冲突时以 docs/42 + 用户当场指示为准。

---

## 0. 开工第一件事（⚠️ 最重要）：git 工作区是脏的，先确认基线

`git status` 显示大量**未提交在途改动**（docs/32-40 计划书未跟踪；README / src / app / frontend / eval 多处 M），且 **`app/api/shenlun.py`（本次要改的核心文件）当前就是 M 状态**。上一会话产物尚未收尾提交（见 CHANGES.md 快照 ⚠️ 行）。

**动作**：开工前先把 `git status` 完整清单与在途 diff 给用户看，请用户决定——
- 哪些在途改动先 commit（建干净基线），哪些继续留工作区；
- 确认 `app/api/shenlun.py` 现有 M 改动内容（`git diff app/api/shenlun.py`），避免 42 的改动与其混在一起无法回滚；
- 本轮 42 落地完成后是否 commit（近期项目有「不主动 commit、攒到功能点」的用户约束——**以用户当场指示为准**，不擅自 commit / push）。

> 行号风险提示：docs/42 §2 的现状行号以 09-07 钉死时为准，若在途改动已漂移，**以代码实际为准**，发现过期顺手更新 docs/42。

## 1. 必读文件（按顺序，开工前读完）

1. `CLAUDE.md` / `AGENTS.md`（会话自动加载）——分工与读写纪律，尤其铁律 1「开工读档」、铁律 2「关窗口写 CHANGES 流水」；
2. **`docs/42-开发计划书-评分单模式化与采分点来源分层.md`（全文，本次任务依据）**；
3. `docs/38`（双模式语义来源：门禁三态 / 灰带 / 建议区；42 部分推翻 D40/D48，需理解被取代的是什么）；
4. `docs/39`（错题本第 5 页签 + wrongbook 内联通道 = L3 确认闸门的复用点）；
5. `docs/CHANGES.md` 顶部快照 + 流水前 3 条（近况；快照已含 42 主线行）；
6. 代码：`app/api/shenlun.py`（路由/入口）、`src/shenlun/score.py`（gate_score）、`src/shenlun/reflow.py`（reflow_answer）、`tests/conftest.py`（全局 mock 约定）。

## 2. 环境与验证命令

- Python：pyproject `requires-python >=3.13`，本机 Anaconda base 即 3.13（`D:/ProgramData/anaconda3/python.exe`）；
- 全量测试：`python -m pytest tests/`——**当前基线 398 passed**（docs/40 落地后口径），改完不许低于此数；
- 语义层 mock 约定：`tests/conftest.py` 全局把 `embed_zh → None`（走线上同款降级路径），**跑测试不需要 Ollama/embedding**；别为单测引入真实 embedding 依赖；
- Lint：`python -m ruff check src/ tests/`（line-length 100）；
- 备份：动 `data/shenlun.db` 前先 `cp` 一份 `.bak`；代码改动建议 git 分支或 stash 留基线。

## 3. 任务范围速览（细则以 docs/42 §4 为准）

| 步 | 内容 | 拍板结论 |
|---|---|---|
| M1 | 后端 submit 单引擎化：删 trusted 路由，恒走 gate | — |
| M2 | `InlineGold` 加 `points_source: manual\|llm_parse`；parse/decompose 响应带标记 | — |
| M3 | 内联题入库闭环：complete 扩展 / wrongbook 通道 | **P-A=②**（见 §4 口径差）|
| M4 | 示证档退役：align_answer 保留标 deprecated，不删 SCORE_FORCE | — |
| M5 | 入库判据对齐：`reflow_answer` 加 verdicts 参数 | **P-B=B1** |
| 交互 | L3「确认无误」按钮（单点粒度）| **P-C=①** |
| 建议 | guidance 对 L1/L2/L3 全放开（L3 措辞加"基于未复核采分点"）| **P-D=①** |

## 4. ⚠️ 计划书内部口径差（落代码前需向用户确认一处）

docs/42 §4.3 流程图写「L2：complete 扩展支持内联 qid → reflow_answer **直接入 weak_points**」，但 **P-A 拍板=②「内联一律只进错题本，不进提醒池」**——两者冲突。**以拍板 ② 为准**：内联题（无论 L2/L3）漏点只进错题本（wrongbook 通道，ctx 已支持 `inline_{hash}`），不进 weak_points 提醒池；弱档案提醒池只服务有题库载体的题。若你认为 L2（用户手写点 = 用户即人审）应有更强回流，**先问用户**，不要自行二选一。

## 5. 硬约束（违反 = 返工）

1. **灰带 LLM 疑似永不硬判、永不自动入库**（judge_suspect 只出 label+reason，无证据字段）——防幻觉内核零改动；
2. **L3 只进错题本、不进 weak_points**（无题库载体 → 提醒"该练 X 点"无题可练的死胡同，docs/42 §4.3 写死）；
3. 记忆铁律不变：只固化「规则可证 ∧ 采分点受信（L1/L2）」的漏答；LLM 疑似/未人审点驱动的判定不自动入库；
4. **无上帝文件、职责单一**（docs/40/41 同款纪律）：新逻辑拆小模块（纯函数无 IO / 单一 IO 面），单文件 ≤80 行量级，别写 300 行全能脚本；
5. **只做 docs/42 圈定范围**：不顺手动 D50 评测重建（docs/38 收尾、并行任务）、不碰 docs/39/41 计划书内容、不重构无关代码；
6. `align_answer` / `SCORE_FORCE` **本期不删**（保留 deprecated，防回滚），回滚路径 = 路由改回 `_mode(trusted)` 一行。

## 6. 实现时的边界注意（docs/42 未完全写死处）

- **「无 points 的内联提交」**（用户没贴标准答案、没给结构化点）：gate 无输入可判。42 说示证退役但没说这分支怎么办——建议默认 400 + 提示"先提供标准答案/采分点"，或按用户意见保留最小兜底；**落代码前确认**；
- **旧前端不带 `points_source`** → 默认 manual（L2）语义变化：旧行为内联根本不回流，新 L2 会回流——**前后端必须同步交付**，别只改后端（docs/42 §7 风险表）；
- docs/40 已 seed 真实库（弱档案 49 漏点/13 题，演示数据）——手工验证时别把它当脏数据清掉；需要干净态先备份再动。

## 7. 验收（docs/42 §6 六条 + 附加）

1. 内联题（带 gold.points）submit 返回 `mode="gate"` + 三色 verdicts，不再出现 `mode="align"`（SCORE_FORCE=align 除外）；
2. L3 判定照给 + 「参考·未复核」标记，**不自动**产生 weak_points 行；
3. L2 按 §4 拍板口径正确落库（默认 = 只进错题本）；wrongbook 内联通道回归通过；
4. M5 落库后：库内题 complete 后 weak_points 的 hit/miss 与 submit 展示 verdicts 一致（抽查 3 题断言）；
5. `python -m pytest tests/` ≥ 398 全绿（双模式旧用例改写：align 用例标 deprecated 或 SCORE_FORCE 锁定）；`ruff` 通过；
6. 前端：模式角标消失、L3「参考·未复核」标记与「确认无误」按钮可见；
7. 手工三路 demo：库内题 / 内联手填 / 内联 LLM 拆 各跑一遍。

## 8. 完工写档（CLAUDE.md 铁律 2）

- 按模板往 `docs/CHANGES.md` 流水区顶部写条目（对应计划 docs/42 §小节 / 改动文件 / 验证命令+结果 / 遗留 / commit hash）；
- docs/42 头部状态行改「已落地」，CHANGES.md 里 D40/D48「被 docs/42 取代」标注；
- 简历联动提醒用户：条目 2/3 的「示证档/双模式」表述待 A 落地后启用目标稿（对话里备好的版本），**那是 WorkBuddy 的事，执行 Agent 不碰简历文件**。

## 9. 明确不做（防 scope 蔓延）

D50 基线重建（docs/38 收尾）、docs/39 落地复核、docs/41 归因闭环、docs/40 seed 产物改动、简历/面试文档、与 42 无关的重构。
