# OfferLoop 模拟面试 · 最小闭环设计

> 一句话定位：**面试官读你的错题本出题 → 追问到你答不上来 → 把新弱点写回错题本；记忆管家算遗忘 → 每天提醒你。** JD 兜底、研究员、教练是后置增量。

这份文档是 grill 定稿后的落地设计。每一步都讲清「输入 / 输出 / 为什么」，方便面试时自己讲得出来。

---

## 1. grill 定稿的 8 个决策

| # | 决策点 | 结论 |
|---|--------|------|
| Q1 | 追问答案对照 | 第一版 LLM 现场生成"期望要点"，第二阶段研究员抽参考答案存库 |
| Q2 | 评估写回 | 复用 review / review_fail 更新 mastery + 新增「行为特征」字段，不加精确算分 |
| Q3 | 两个 Agent | 两个角色（独立 prompt + 工具集 + 状态）共享记忆层，不做独立进程 |
| Q4 | 出题源 | 只考错题本 fail/partial（gap 排序 top N），JD 兜底后置 |
| Q5 | 交互形式 | CLI 打字版 + 傻瓜化包装，前端暂不做 |
| Q6 | 面试规模 | 一场 5 题，每题最多追 2 轮，5 题全结束输出评估报告 |
| Q7 | 追问禁模板 | LLM 输出结构化判断，系统只卡「轮次上限 + 跨题阻断」两个硬约束 |
| Q8 | 行为特征 | 面试结束统一总结，3 维度：答不到点 / 表达绕弯 / 回避问题 |

---

## 2. 整体架构

```
你（人，环内亲口答题）
   │
   ├── 面试官 Agent（新建）      ← 工具：读错题 / 检索答案对照 / 写错题
   │
   └── 记忆管家 Agent（已有 80%）← 工具：算遗忘 gap / 发每日提醒
          │
          └── 共享记忆：错题本（Chroma）
```

- **两个 Agent 不直接通信**，全靠读写同一个错题本协同。
- 面试官考完 → 写回错题本（mastery 变化 + 行为特征）→ 记忆管家读到 → 算遗忘 → 提醒你。

---

## 3. 数据模型改动（最小）

**唯一的新字段**：`KnowledgeItem.behavior_tags: list[str]`，默认空列表。
存的是行为特征标签，如 `["表达绕弯", "回避问题"]`。

> 为什么只加一个字段：错题本已有 question / status / mastery / gap / history / user_note，够用了。行为特征是"记人"的那一层（TechSpar 的人级画像），是唯一缺的。

**面试 session 状态**（内存态，不持久化到 Chroma）：
- `current_question`：当前题
- `current_round`：当前追到第几轮
- `asked_ids`：已问过的题（避免重复）
- `accumulated_behaviors`：累计行为特征（结束时总结用）

> 为什么不持久化 session：面试过程是短生命周期的，断了重来就行；错题本才是有长期价值、要持久化的东西。

---

## 4. 面试官 Agent 状态机（核心）

```
选下一题 → 出题 → 等回答 → 追问判断 ──追问──→ 出题（带着追问方向）
                        │
                        └──完成──→ 还有题？──是──→ 选下一题
                                            └否→ 评估报告（写回错题本）
```

每一步的输入 / 输出：

| 步骤 | 输入 | 输出 | 用什么工具 |
|------|------|------|-----------|
| 选下一题 | 错题本 fail/partial 全集 | gap 排序 top 5 里还没问过的题 | 读错题 |
| 出题 | 当前题 + 期望要点 | 面试官的问题（或追问） | 检索答案对照 |
| 等回答 | 用户打字回答 | 转追问判断 | — |
| 追问判断 | 题 + 期望要点 + 回答 + 轮次 | `{需要追问, 追问方向, 判断依据}` | LLM |
| 评估报告 | 5 题的问答记录 | 逐题 mastery 更新 + 行为特征 | 写错题 |

---

## 5. 三个工具（tool calling 的落地）

| 工具 | 函数 | 输入 → 输出 | 实现 |
|------|------|------------|------|
| 读错题 | `get_weak_questions()` | 无 → top 5 弱题 | 复用 `store.search` + `mastery.rank` |
| 检索答案对照 | `get_expected_points(q)` | 题 → 期望要点列表 | 第一版 LLM 生成，第二阶段查知识库 |
| 写错题 | `record_result(id, 好/差, behaviors)` | 结果 → 更新后的 item | 复用 `review` / `review_fail` + 追加 behavior_tags |

> 为什么这三个必须是"工具"而不是写死在代码里：因为它们是 LLM 的手。没有"检索答案对照"，面试官凭感觉打分 → 幻觉；没有"写错题"，错题就得你手动标 → 退回工具。

---

## 6. 追问判断（Q7 落地，灵魂）

每轮回答后，面试官调 LLM，**强制输出结构化判断**：

```json
{
  "需要追问": true,
  "追问方向": "追问你刚才没提到的三次握手为什么是三次",
  "判断依据": "回答只说了建立连接，没讲清为什么不是两次"
}
```

系统只卡两个硬约束，其余全交 LLM：
1. **轮次上限**：追满 2 轮，无论好坏强制进入下一题。
2. **跨题阻断**：追问方向不得引用其他题的内容。

> 这就是"禁模板"的真正落地：规则只守住安全边界，判断全在 LLM。面试时一句话讲清："我的追问不是 if-else，是每轮让面试官输出结构化判断，系统只卡轮次上限和跨题安全。"

---

## 7. 记忆管家 Agent（已有 80%，只接线）

- **现状**：`run_remind.py --notify` 已经会"检索 fail/partial → 算 gap → 对 gap≥0.5 的题弹 toast 提醒"。
- **Agent 化**：把它包装成"记忆管家"这个角色，给它一个独立的 prompt（"维护记忆、按遗忘安排复习"）+ 两个工具（算 gap、发提醒）。

> 第一版其实不用大改代码，主要是**叙事上**把它定义成一个 Agent 角色。面试时你说："记忆管家负责记忆的主动性，面试官负责训练"——两者的职责边界清晰。

---

## 8. 落地顺序（每个都是小步，可单独验证）

1. **加字段**：`KnowledgeItem.behavior_tags`（改 schema + store 的 metadata 序列化）。
2. **写面试官状态机**：`run_mock_interview.py`，先跑通"选题 → 出题 → 等回答 → 追问判断"的单题循环。
3. **接评估写回**：面试结束调 `review`/`review_fail` + 写 `behavior_tags`。
4. **接线记忆管家**：复用 `run_remind.py`，面试后新写入的弱点能被下一次提醒抓到。
5. **CLI 傻瓜化**：一条命令启动，清晰的"第 X/5 题"提示。

> 为什么按这个顺序：每一步都能单独验证、单独 commit，符合"小改动攒着、完整功能点才 commit"的约定；也让你每做一步都能对着讲。
