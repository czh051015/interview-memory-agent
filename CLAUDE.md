# CLAUDE.md — offerloop 开发说明（PointLoop · 逐点）

## 协作模式（本项目的固定工作流）
本项目由 **WorkBuddy** 与 **Claude Code** 协作开发，用户是最终决策者。你（读本文件的 Agent）是 **WorkBuddy / Claude Code 侧**，职责分工：

- **用户**：在对话里提出需求 → 拍板决策 → 指定执行哪份计划书。**"本轮做哪份计划"由用户当场决定，不由任何文件自动推定。**
- **WorkBuddy**：帮用户理解、制定 / 更新计划书（`docs/NN-开发计划书-*.md` / `docs/39-修改计划书-*.md`，按用户要求新建或改现有那份）、验收交付、把验收结论回写批注。**每轮对话结束（关窗口前）：把本轮沉淀的重要结论（项目决策 / 新坑 / 主线变化）按流水模板写入 `docs/CHANGES.md`，一轮可写多条**——流水区是双方共同的"近况"，双方都写。
- **Claude Code**：按**用户指定的那份计划书**实现代码、跑验证、写 `docs/CHANGES.md` 流水条目、commit。Claude Code 不自己挑计划书。

这套体系的收益：**WorkBuddy 不用二次验证 Claude Code 的全部工作**（读流水条目定向核对即可）；**Claude Code 不用重读整个项目**（开工只读：用户指定计划书 + CHANGES 快照 + 流水前 3 条）。收益能否兑现，取决于流水条目质量与快照维护纪律。

## 文件地图（路径相对仓库根）
| 文件 | 内容 | 谁写 | 何时读 |
|---|---|---|---|
| `docs/CHANGES.md` | **共享记录**：顶部「当前快照」= 当前主线等动态状态（精简，README 有的不重复）；下方「变更流水」= 每轮工作记录 | 快照：谁改变主线谁更新（覆盖式）；流水：Claude Code 与 WorkBuddy 关窗口都写 | 每次开工（快照 + 流水前 3 条） |
| `docs/NN-开发计划书-*.md` 等 | **各主题计划书**（每轮一份，编号 01-39+，如 `docs/38-开发计划书-评分引擎混合型…` / `docs/39-修改计划书-申论错题本…` / `docs/实施计划-申论工作台三项闭环优化.md`）——目标 / 范围 / 验收标准 / 状态行 | WorkBuddy 起草，用户拍板 | Claude Code 按用户指定那份读；WorkBuddy 制定时读关联计划书 |
| `README.md` | 完整运行手册（架构 / 命令 / 设计约定 / 坑，权威） | — | 需要项目知识时读，Claude Code 开工按需 |
| `CLAUDE.md` / `AGENTS.md` | 分工与读写纪律（本文件与执行侧手册） | 用户定稿 | 各自每次会话自动加载 |

**事实源兜底**：任何 md 与 git / 代码不一致时，以 git 与代码为准，md 可重建——发现过期就更新，不将就。

## 铁律
1. **开工前读档**：Claude Code 读「用户指定的计划书 + `docs/CHANGES.md`（顶部快照 = 动态现状 + 流水前 3 条 = 近况）」，再 `git log --oneline -10` 核对实际变更；需要项目知识时读 `README.md`，不要一上来重读整个项目。计划书是哪份，以用户当场指示为准，不要自动假设。
2. **关窗口写档**：Claude Code 干完一轮 / WorkBuddy 每轮对话结束（关窗口前），都按模板往 `docs/CHANGES.md` 流水区顶部写条目（四要素：对应计划 / 改动 / 验证 / 遗留），一轮可写多条，commit 短 hash 回填（纯规划 / 验收条目无 commit 则注明）。
3. **角色边界**：Claude Code 只实现用户指定计划书圈定的范围；遇到范围外的架构 / 接口决策，停下问用户，不擅自扩大。WorkBuddy 不越俎代庖替 Claude Code 写业务代码。
4. **验收闭环**：WorkBuddy 收到交接后定向核对（diff + 跑验证），结论以「验收批注」写回该条目，Claude Code 下一轮直接继承，不必重新发现。
5. **文档瘦身**：快照**只留 README 没有的动态状态**（当前主线等），README 覆盖的知识一律不重复；快照覆盖式维护，禁止 append 历史。流水区旧条目堆积后归档；历史计划书完成即存档（状态行标「已完成」），不留"假活档"误导下轮。
