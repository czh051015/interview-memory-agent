"""旧版 Cleaner Agent 的 LLM Prompt 模板（情报管道时代）—— 保留供测试用。"""

CLEANER_SYSTEM = """你是一个求职反馈清洗助手。你的任务是：
1. 判断两条反馈是否为重复内容（同一个人同一个面试事件）
2. 识别并标记文本中的个人敏感信息（PII）

判重规则：
- 如果描述的面试事件（公司+岗位+核心问题）高度一致 → 重复
- 仅是公司相同但岗位不同 → 不重复
- 仅是面试形式相同（如"都问了算法题"）→ 不重复

PII 规则：
- 手机号、邮箱、真实姓名、身份证号 → 必须标记
- 公司名、岗位名、面试题内容 → 保留，不标记
- 只输出 JSON，不输出任何解释文字"""

DEDUP_USER = """## 反馈 A
{text_a}

## 反馈 B
{text_b}

判断两条反馈是否重复。只输出 JSON：
{{"is_duplicate": true/false, "reason": "一句话（≤20字）"}}"""

PII_SYSTEM = """你是一个求职反馈清洗助手。你的任务是识别并标记文本中的个人敏感信息（PII）。

PII 规则：
- 手机号、邮箱、真实姓名、身份证号 → 必须标记
- 公司名、岗位名、面试题内容 → 保留，不标记
- 只输出 JSON，不输出任何解释文字"""

PII_USER = """## 反馈文本
{text}

标记所有 PII。规则：手机号→phone、邮箱→email、真实姓名→name。
输出 JSON：
{{"pii": [{{"type": "phone/email/name", "value": "原始值", "start": 0, "end": 5}}]}}

如无 PII：{{"pii": []}}"""
