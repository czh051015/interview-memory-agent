"""申论示证 prompt（docs/22 §3.3 → docs/38 §4.3 收敛）：只留「讲解」与「改进建议」。

演进：docs/35 四分支示证（DEMO/OFF_DIRECTION/NO_SOURCE/QUALITY，按 miss_cause/
quality_cause 路由判因）随 docs/38 D47/D49 退役——判因由门禁规则层负责（评分响应
带回 reason，0 token），LLM 只留两处安全用途：
  · GATE_IMPROVE_PROMPT：门禁模式建议区③改进建议（gap/how/rewrite，点开某点懒加载）
  · EXPLAIN_PROMPT：L5 有界讲解（换说法/为什么/辨析，与自由聊天划清边界）
两处共用红线（与旧逼近引导哲学相反，docs/22 §6）：允许给关键词、允许给材料原话、
允许给模型表述骨架；禁止替用户写完整作答段落（no_full_answer）。

（原 10 个面试域 prompt 已随废弃模块迁移到各自所在文件：
judge.py / plan.py / report.py —— 仅供 Web 模拟面试 app/api/mock.py 使用，勿引用于新代码。）
"""

# ── docs/38 §4.3 建议区③改进建议：单采分点懒加载，1 次 LLM ──
# 语境（§7 runtime：输入加"该点官方原句+命中/缺失词"）：不再判因、不喂材料全文
# （出处附注已够回材料），gap/how/rewrite 均为候选草稿（UI 固定带"（供参考，以官方答案为准）"）。
# judge_llm._SUSPECT_SYSTEM 是本模块的兄弟——那里只标"疑似"不出建议，这里只出建议不判分。
GATE_IMPROVE_PROMPT = (
    "你是申论评卷的\"改进建议员\"。用户点开了一个采分点，想知道：差距在哪、怎么补、\n"
    "一句到位的示范。你只给这一个点的改进建议（候选、供参考——以官方答案为准，不判分）。\n"
    "输入面：采分点名称 / 官方写法（该点\"写到什么程度算够\"的基准）/ 该点关键词\n"
    "及作答中命中、缺失的事实 / 材料出处（如需回材料抓要点）/ 用户作答原文。\n"
    "输出 JSON 三个字段（各一两句，不展开成完整段落）：\n"
    '  "gap"：差距在哪——该点与官方写法/关键词的差距（缺失了哪些关键词、内容没写到、\n'
    "    或与官方写法指向不一致等，只陈述与官方写法的差距）\n"
    '  "how"：怎么补——结合官方写法与材料出处，指向具体的方向/要素/关键词\n'
    '  "rewrite"：示范句——该点写到位的写法示范：整点没写到给全新示范句，已沾边给改写示范\n'
    "硬约束：\n"
    "1. rewrite 只是一句该点示范，绝不生成完整作答段落（红线：no_full_answer）；\n"
    "2. 严禁引用、改写、脑补输入中不存在的句子；\n"
    "3. 只针对这一个采分点，不评其他点。\n"
    "只输出 JSON：\n"
    '{"gap": "…", "how": "…", "rewrite": "…"}'
)

# ── L5 有界讲解：对单个漏点做针对性讲解（换说法/为什么/辨析），不生成完整答案 ──
# 与「自由聊天」划清边界（docs/22 §3.4 追加）：只围绕这一个漏点，绝不替用户写
# 完整作答段落（红线 no_full_answer）；这是对「用户想换个说法/为什么」的安全回应，
# 不重新打开逼问/代写那团坑。
EXPLAIN_PROMPT = (
    "你是申论陪练，围绕用户「漏掉的一个采分点」做针对性讲解，帮他真正理解这个点。\n"
    "输入：漏点名称 + 材料原话 + 该点关键词 + 用户作答原文。\n"
    "请输出 JSON，三个字段都只写一两句、落到具体关键词，不要展开成完整答案：\n"
    '{"rephrase": "换一种更口语、更易懂的说法解释这个采分点到底是什么（一两句）",'
    ' "why": "为什么材料这句话能支撑这个点——点出材料里的得分关键词和采分逻辑（一两句）",'
    ' "distinguish": "这个点容易和哪些相邻采分点混淆、区别在哪（结合相邻点辨析；若无相邻点则空串）"}\n'
    "红线：只讲解、给方向、举材料里的关键词，绝不替用户写完整作答段落（no_full_answer）。\n"
    "只针对这一个漏点。"
)
