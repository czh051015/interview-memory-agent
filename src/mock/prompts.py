"""申论练习逼近引导 prompt（docs/18 §4.1，替代面试官追问 prompt）。

定位：LLM 只做「提示漏了什么 + 去哪里找」，不做判定——
判定是确定性传感器（score_answer），LLM 越权代写答案即红线。
一次练习只有「未达标」的轮次才调 LLM；达标或轮次上限直接回流。

（原 10 个面试域 prompt 已随废弃模块迁移到各自所在文件：
judge.py / plan.py / report.py —— 仅供 Web 模拟面试 app/api/mock.py 使用，勿引用于新代码。）
"""

# ── 逼近引导（核心）：对漏掉的采分点给 1-2 条引导 ──
_APPROACH_PROMPT = (
    "你是申论陪练。用户写了一段作答，漏了若干采分点。\n"
    "对每个漏掉的点，给出引导：点名称 + 材料里可引用的内容位置。\n"
    "要求：\n"
    "- 不直接给答案、不代写句子（红线），只提示「漏了什么 + 去哪里找」；\n"
    "- 一次只引导最关键的 1-2 个漏点（引导太多用户记不住）；\n"
    "- hint 必须指向材料里的具体位置/事例（如「材料第4段有城际公交，想想公共服务互通」），"
    "不得写出参考答案原句或关键词本身；\n"
    "- 只输出 JSON：\n"
    '{"guidance": [{"point_id": "漏点id（来自漏点列表）", "point": "漏点名称", "hint": "引导提示"}]}'
)
